/* exported getValue */
function getValue(object, key) {
  return object[key];
}

/*
1. Upon receiving an entity or entities with its traits and its specific descriptions
2. define only its specific descriptions
3. move its specific descriptions to output
*/
