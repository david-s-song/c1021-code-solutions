/* exported findIndex */
function findIndex(array, value) {
  var index = [];
  for (var i = 0; i < array.length; i++) {
    array.find(value[0] = 1);
  } return index;
}

/*
steps
1. defined function
2. created an empty variable
3. using for loop (condition) to see each of the items in array using .length
4. incrementing to check each of the items in array
5. using find method to 'find' parameter value at 0 index and assinging it to 1...

*/
