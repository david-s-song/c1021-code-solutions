/* exported firstChars */
function firstChars(length, string) {
  return string.slice(0, length);
}




/*

pseudocode
1. create a storage container
2. look through the number of characters in the sentence
3. place the first character into the storage container
4. place the next one into the storage container. Repeat this process until the limit of the count is reached
6. Put the storage container as output

*/
