/* exported getLastChar */
function getLastChar(string) {
  var lastChar = string[string.length - 1];
  return lastChar;
}

/*
Pseudo-Code
1. create a storage place for output
2. grab the last character of the word by counting each of its character
and subtracting from the last one.
3. give back the output of the storage unit created

*/
