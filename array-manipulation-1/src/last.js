/* exported last */
function last(array) {
  return array[array.length - 1];
}

/*
1. Identify the items in the container
2. Count the number of items in the container.
3. Identify the last item in the container and move back one position from the last item
4. Put the output as the item previously identified
*/
